import P from 'prop-types';

import { Heading, Sidebar } from '../../components';
import { Base } from '../../templates';

const Home = () => {
  return (
    <Base>
      <Sidebar />
    </Base>
  );
};

export { Home };
