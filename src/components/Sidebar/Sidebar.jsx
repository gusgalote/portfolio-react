import P from 'prop-types';

import * as Styled from './Sidebar.styles';

const Sidebar = () => {
  return (
    <Styled.Container>
      <Styled.Box>oi</Styled.Box>
    </Styled.Container>
  );
};

export { Sidebar };
